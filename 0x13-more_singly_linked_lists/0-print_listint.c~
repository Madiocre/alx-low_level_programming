#include "lists.h"

/**
 *
 */

size_t print_listint(const listint_t *h)
{
const listint_t *current = h;
size_t count;
count = 0;

while (current != NULL)
{
if (current->n != NULL)
{
printf("%u\n", current->n);
}

current = current->next;
count++;
}

return (count);
}
